/*

const student = {
    name: 'John',
    age: 20,
    score: 8.5
};
console.log('Student ' + student.name + ' has a score of ' + student.score);

student.score = 10;
student.class = '12A';
console.log('Student after update:', student);
*/

const config = {
    'server name': 'Google'
};
console.log(config['server name']);
delete student.age;
console.log('Student after deleting age:', student);